# NovaPOS — App (Flutter)

Cliente Flutter de NovaPOS — Android, Windows Desktop y Web desde el mismo código. Repo separado del backend (`NovaPOS-Code`) a propósito, para versionar/desplegar cada uno por su cuenta.

## Cómo levantarlo

**1. Backend** (`NovaPOS.Api`) — en `NovaPOS-Code`:

```bash
dotnet run --project src/NovaPOS.Api
```

Si el certificado de desarrollo HTTPS da problemas (`dotnet dev-certs` no confiado), forzar HTTP explícito:

```bash
ASPNETCORE_URLS="http://localhost:6453" ASPNETCORE_ENVIRONMENT="Development" dotnet run --project src/NovaPOS.Api --no-launch-profile
```

Por defecto la app apunta a `http://localhost:6453/api/v1` (o `http://10.0.2.2:6453/api/v1` automáticamente si corre en el emulador de Android, ver `lib/core/config/api_config.dart`). Para apuntar a otro servidor:

```bash
flutter run --dart-define=API_BASE_URL=https://api.novapos.cl/api/v1
```

En Development, `NovaPOS.Api` necesita CORS habilitado para aceptar llamadas desde el origen del servidor Flutter Web (puerto distinto al de la API) — ya viene configurado en el backend (`app.UseCors("DesarrolloFrontend")`, solo en Development).

**2. App Flutter** — en este repo:

```bash
flutter pub get
flutter devices          # lista los destinos disponibles
```

| Destino | Comando |
|---|---|
| Navegador (Chrome) | `flutter run -d chrome` |
| Windows Desktop | `flutter run -d windows` |
| Android (emulador/dispositivo) | `flutter run -d <id-del-emulador>` |

**Si en Chrome las llamadas al backend fallan con "No se pudo conectar con el servidor" pese a que el backend responde bien** (confirmable con `curl`/Swagger): el SDK de Flutter de este proyecto es de 2023 y su compilador de modo debug (DDC) depende de `Intl.v8BreakIterator`, una función que Chrome ya eliminó en versiones recientes. Corre en modo release en su lugar (pierde hot reload, pero evita el problema):

```bash
flutter run -d chrome --release
```

**Cuenta de prueba fija** (para no tener que registrar una Empresa nueva cada vez — ver detalle en el README de `NovaPOS-Code`, no eliminar de la LocalDB). Ya tiene catálogo y Clientes de ejemplo cargados:

| Campo | Valor |
|---|---|
| RUT | `81.814.677-9` |
| Correo | `admin@novapos-demo.cl` |
| Contraseña | `Demo1234!` |

La solución definitiva es actualizar el SDK de Flutter, no hecho todavía en este repo.

Para probar el Punto de Venta hace falta una Empresa ya registrada (pantalla Registro de Empresa) con al menos una Caja cargada (la que UC-01 auto-provisiona alcanza) y un Producto — este último ya se puede crear desde la propia app, en Home → Catálogo.

## Arquitectura

Clean Architecture por feature, mismo espíritu que el backend (`domain` → contratos y modelos sin dependencias externas, `data` → implementación concreta contra la API, `presentation` → widgets + estado):

```
lib/
  core/                    # Compartido entre features
    config/                # ApiConfig — URL base del backend
    network/               # ApiClient (Dio), AuthInterceptor (adjunta/refresca el token)
    storage/               # TokenStorage + SecureStorage (abstracción sobre flutter_secure_storage)
    router/                # GoRouter con redirect automático según sesión
    theme/                 # AppTheme único
    providers/             # Providers de Riverpod compartidos (tokenStorage, apiClient)
  features/
    auth/
      domain/              # AuthRepository (contrato), modelos (AutenticacionResult, etc.)
      data/                # AuthApi (llamadas HTTP crudas), AuthRepositoryImpl
      presentation/
        providers/         # AuthController, RegistroEmpresaController (StateNotifier)
        screens/           # LoginScreen, RegistroEmpresaScreen, SplashScreen
    catalog/
      domain/              # CatalogRepository/CatalogAdminRepository (contratos), ProductoVendible, ProductoAdmin, Departamento/SubDepartamento/Clase/Subclase/Marca
      data/                # CatalogoApi (búsqueda POS), CatalogAdminApi (alta/edición/activar/desactivar)
      presentation/
        providers/         # ProductosAdminController (listado), CatalogFormController (cascada de clasificación + alta)
        screens/           # ProductosAdminScreen (listado), CatalogFormScreen (alta de Producto)
        widgets/           # SelectorConAlta, ClasificacionCascade, EditarProductoDialog, EditarVarianteDialog
    tenancy/
      domain/              # TenancyRepository (contrato), CajaResumen, BodegaVenta
      data/                # TenancyApi, TenancyRepositoryImpl
    inventory/
      domain/              # InventoryRepository (contrato), StockVariante
      data/                # InventarioApi, InventoryRepositoryImpl
    customers/
      domain/              # CustomerRepository (contrato), ClienteResumen
      data/                # CustomerApi, CustomerRepositoryImpl
    sales/
      domain/              # SalesRepository (contrato), LineaCarrito, FormaPago/TipoEntrega
      data/                # VentaApi, SalesRepositoryImpl
      presentation/
        providers/         # BusquedaProductosController (debounce + categoría + stock), BusquedaClientesController, PosCartController (carrito + cobro)
        screens/           # PosScreen — Punto de Venta
        widgets/           # SelectorClienteDialog
    home/
      presentation/screens/  # Menú post-login — punto de entrada a los demás features
```

- **Estado**: Riverpod (`flutter_riverpod`), patrón `StateNotifier` clásico — sin codegen, sin `build_runner`.
- **Navegación**: `go_router`, con `redirect` que observa el estado de sesión (`AuthController`) y manda automáticamente a Login/Home según corresponda.
- **HTTP**: `dio`, un único `ApiClient` compartido por todos los features con `AuthInterceptor` — adjunta el Access Token a cada request y, si el backend responde 401, intenta refrescarlo una vez con el Refresh Token antes de reintentar la request original.
- **Sesión**: `flutter_secure_storage` vía la abstracción `SecureStorage` — permite reemplazarlo por un fake en memoria en los tests (el canal de plataforma real no existe bajo `flutter test`).
- **RUT chileno**: `RutValidator` (`lib/core/utils/rut_validator.dart`) replica el mismo algoritmo de módulo 11 que el backend (`RutChileno.cs`) — nunca se le pide al servidor validar algo que el cliente ya puede rechazar.

## Qué está implementado

- **Login**: RUT + correo + contraseña contra `POST /auth/login`. Si hay una sesión guardada y vigente, la app entra directo a Home sin mostrar Login.
- **Registro de Empresa**: formulario completo (datos de la Empresa + cuenta del Administrador) contra `POST /empresas` — implementa UC-01 del backend. No deja sesión iniciada (mismo criterio que el backend: el JWT se emite recién en Login, no en el registro); al terminar, muestra un diálogo de confirmación y vuelve a Login.
- **Refresh de token automático**: si el backend responde 401 en cualquier llamada, `AuthInterceptor` intenta refrescar una vez con el Refresh Token guardado y reintenta la request original — transparente para el resto de la app.
- **Punto de Venta (POS)**: búsqueda de productos con debounce (`GET /catalogo/productos`), selección de Caja (automática si la Empresa tiene una sola, selector si tiene varias — `GET /cajas`), carrito 100% local (el backend no soporta editar/quitar una línea de Venta ya agregada, así que armar el carrito contra la API línea a línea no es seguro), y al presionar "Cobrar" se ejecuta la secuencia `crearVenta → agregarLinea (por cada línea) → confirmarVenta`. Si la secuencia falla a mitad de camino, el error se muestra y el carrito local NO se vacía (para reintentar sin volver a tipear todo) — la Venta puede quedar en Borrador con líneas parciales en el servidor, limitación conocida y aceptada mientras no exista un endpoint para editar/cancelar una Venta en Borrador.
- **Categorías y stock en el POS**: tabs de filtro por Departamento arriba del buscador (`GET /catalogo/departamentos`, reutilizado del feature de administración de Catálogo) y badge de stock en cada tarjeta de resultado (`POST /inventario/bodegas/{id}/existencias/consultar`, en batch para todos los resultados de una búsqueda a la vez). La Bodega de venta se resuelve una sola vez por sesión de POS a partir de la Sucursal de la Caja seleccionada (`GET /bodegas/venta?sucursalId=`) — mientras no se resuelva, o si la consulta de stock falla, los resultados de búsqueda igual se muestran, solo sin el badge (el stock es puramente informativo, nunca bloquea la venta).
- **Selector de Cliente en el POS**: fila arriba del buscador con el Cliente actual ("Cliente Genérico" por defecto) — tocarla abre `SelectorClienteDialog`, un buscador con debounce (`GET /clientes/buscar?texto=`) igual que el de productos. Elegir un Cliente, tocar "Usar Cliente Genérico" o cerrar el diálogo de cualquier forma deja seleccionado el Cliente Genérico salvo que se haya tocado explícitamente un resultado — simplificación a propósito, no hay un tercer estado "no cambiar nada". El `clienteId` elegido (o `null`) se pasa a `crearVenta`, y se limpia automáticamente después de cada cobro para no arrastrarlo a la siguiente Venta.
- **Desglose Neto/IVA en el POS**: `ResumenVenta` (`neto`/`iva`/`total`) — mientras se arma el carrito se calcula en el cliente con la misma fórmula que el backend (`ResumenVenta.calcular`, 19% con precios IVA incluido); al cobrar, el diálogo de confirmación muestra el desglose real devuelto por `POST /ventas/{id}/confirmar` (`{ neto, iva, total }`), no el estimado local — evita que un redondeo de último momento en el servidor se muestre distinto sin que el Cajero lo note.
- **Escaneo de código de barras en el POS**: botón de cámara (`mobile_scanner`) junto al buscador — abre `EscanearCodigoBarraScreen` en pantalla completa y, al detectar un código, busca ese texto exacto y, si hay una única Variante cuyo `codigoBarras` coincide exactamente, la agrega directo al carrito sin que el Cajero tenga que elegir entre resultados (sin coincidencia exacta, se avisa con un SnackBar en vez de agregar cualquier cosa). La función que abre la cámara real es inyectable en `PosScreen` (mismo criterio que `SecureStorage`/`InMemorySecureStorage`) — los widget tests simulan un escaneo sin tocar la cámara real. **No disponible en Windows desktop** (`mobile_scanner` no tiene implementación ahí): el botón se oculta solo en esa plataforma — un lector físico de código de barras ya funciona como teclado sobre el mismo campo de búsqueda, así que no se pierde la función, solo la variante por cámara. Requirió subir `compileSdkVersion` a 34 y `minSdkVersion` a 21 (Android 5.0+) en `android/app/build.gradle`, y Kotlin de 1.7.10 a 1.9.10 en `android/build.gradle` (evita un choque de clases duplicadas de `kotlin-stdlib` entre la versión que trae `mobile_scanner` y la que traía el proyecto) — verificado con `flutter build apk --debug`, `flutter build web` y `flutter build windows`, los tres compilan limpio.
- **Rediseño visual del POS** (a pedido explícito, con mockup de referencia): marco oscuro (navy) para el header y el panel de carrito, envolviendo un área de trabajo clara con la grilla de productos — mismo patrón visual que una caja registradora física. Grilla de tarjetas de producto (en vez de lista) con badge de stock semáforo (verde/ámbar/rojo), buscador con botón "Escanear" separado, chips de categoría restyleados, y el panel de carrito fijo a la derecha con Cliente, líneas, aviso si alguna línea del carrito no tiene stock (`⚠ ... sin stock — se permitirá la venta`, informativo, nunca bloquea), y desglose Subtotal/IVA/Total. Alcance deliberadamente **solo visual**: los botones "+ Descuento" y "Cotización" del mockup quedan visibles pero deshabilitados (con tooltip "Próximamente"), porque esa lógica no existe en el backend todavía — ver "Qué falta". El botón "Vaciar" sí quedó funcional (`PosCartController.vaciarCarrito()`), al ser una acción puramente local sin dependencia de backend.
- **Venta por peso/volumen (PPUM) en el POS**: para Productos cuya `UnidadMedida` es Kilogramo o Litro, `Producto.PrecioVenta` ya representa directamente el Precio por Unidad de Medida (el concepto de transparencia al consumidor: precio por kilo/litro, sin necesidad de un campo adicional de "contenido neto"), así que solo hizo falta trabajo de UI. Las tarjetas de resultado muestran el precio como "$X/kg" o "$X/L" en vez del precio plano, y tocar una de estas tarjetas (o escanear su código de barras) abre `CantidadPesableDialog` pidiendo el peso o volumen exacto **antes** de agregar al carrito — igual que pesar en una balanza real — en vez de agregar con cantidad 1 y tener que corregir después. La línea del carrito para estos productos reemplaza los botones +/- (que solo tiene sentido para unidades enteras) por un valor tocable ("0.35 kg ✎") que reabre el mismo diálogo, precargado, para corregir el peso. Agregar dos veces el mismo Producto pesable **suma** al peso ya cargado en la línea (permite pesar el mismo artículo suelto en varias tandas), en vez de reemplazarlo. Se introdujo el enum compartido `UnidadMedida` (`lib/features/catalog/domain/models/unidad_medida.dart`) para eliminar la lista `_unidadesMedida` que estaba duplicada en `CatalogFormScreen` y `EditarVarianteDialog`.
- **Catálogo (administración)**: listado de Productos con sus Variantes anidadas (`GET /catalogo/productos/todos`, incluye inactivos), activar/desactivar cada uno con un `Switch` (recarga la lista completa tras cada cambio, sin mutación optimista — catálogo de tamaño de PyME, costo bajo), y alta de Producto + primera Variante (`CatalogFormScreen`) con los 5 combos de clasificación en cascada (Departamento→SubDepartamento→Clase→Subclase, y Marca) — cada combo tiene una opción "+ Nueva..." que crea el nivel al vuelo (`SelectorConAlta`), necesario porque una Empresa recién registrada no tiene ninguna Categoría cargada. Editar un Producto (nombre/descripción/clasificación) o una Variante (precio/unidad/código de barras/color/talla/ubicación) se hace con diálogos chicos desde el listado — el `Sku` nunca es editable, igual que en el backend. **Limitación conocida**: al editar un Producto, la clasificación actual se muestra solo como texto ("Manga Corta · Nike"); si se quiere cambiar, hay que recorrer la cascada completa desde Departamento — el backend no expone un endpoint para resolver la ascendencia completa de una Subclase directamente.
- **Con qué Usuario y Empresa se inició sesión**: el `AppBar` de Home muestra "Nombre completo · Razón social" bajo el título — viene de los 3 campos nuevos que `POST /auth/login` agrega a su respuesta (`nombreCompleto`/`email`/`empresaRazonSocial`, ver README del backend). Se guardan en `TokenStorage` junto a los tokens (el refresh los deja `null` a propósito, así que no se sobrescriben en cada renovación silenciosa) y se leen de ahí al restaurar una sesión guardada al abrir la app — no hace falta volver a llamar a `/auth/login` para saber quién es el Usuario actual. `AuthRepository.login()` pasó de `Future<void>` a `Future<SesionUsuario>`, y se agregó `obtenerSesionActual()` al contrato.
- **Descuento por volumen en el POS y Catálogo** (a pedido explícito, ej. "Desde 15 unidades obtén un 5% de dto." — patrón visto en Sodimac/Easy): `CatalogFormScreen` (alta) y `EditarVarianteDialog` (edición) ganan 2 campos opcionales — cantidad mínima y % de descuento — validados como "ambos o ninguno" del lado del cliente (mismo criterio que `VarianteProducto.ValidarDescuentoVolumen` en el backend). En el POS, `ProductoResultadoTile` muestra un aviso verde ("Desde 15 uds. -5%") en la tarjeta del producto cuando tiene la regla configurada, y `LineaCarrito.subtotal` la aplica **automáticamente** apenas la cantidad de esa línea alcanza el umbral — sin que el Cajero tenga que hacer nada — porque el carrito es 100% local hasta el Cobrar (ver "Punto de Venta (POS)" más abajo), así que el mismo cálculo que hará el backend al agregar la línea real se estima antes, del lado del cliente. `CarritoLineaTile` muestra "X% dto. por volumen aplicado" bajo la cantidad cuando corresponde. Verificado con 2 widget tests nuevos (aviso visible en la tarjeta; el Total del carrito pasa de sin-descuento a con-descuento exactamente al cruzar el umbral, no antes) y `flutter analyze` limpio.
- **Tocar la cantidad de una línea por Unidad para tipear un número grande** (a pedido explícito, ej. "una Empresa que pide 2000 sacos de cemento" — tocar "+" 2000 veces no es razonable): el diálogo que ya existía solo para productos por Kilogramo/Litro (`CantidadPesableDialog`) se generalizó para poder tipear también una cantidad entera exacta en productos por Unidad — se adapta según `producto.unidad.esPesable` (con o sin sufijo de unidad en el label, decimal habilitado o no, exige número entero cuando no es pesable). En `CarritoLineaTile`, el número de la línea (antes texto fijo entre los botones +/-) ahora es tocable (`carritoCantidadUnidad`) y abre el mismo diálogo precargado con la cantidad actual — los botones +/- siguen ahí para ajustes rápidos de a 1. De paso se corrigió un overflow de layout: con una cantidad de varios dígitos, el precio unitario ("x $1.500") ahora se trunca con `TextOverflow.ellipsis` en vez de desbordar la fila. Verificado con 2 widget tests nuevos (tipear 2000 y ver el Total recalculado; rechazar un número no entero) y `flutter analyze` limpio.
- **Promociones por grupo en el POS y Catálogo** (a pedido explícito, "2x1, 6x5 y segundo producto 40% descuento"): mismo mecanismo genérico que el backend — cada N unidades compradas, la última recibe X% de descuento (ver README del backend) — expuesto en la UI con **ambos** niveles a la vez: los 2 campos genéricos crudos y, encima, `PromocionGrupoField` (`lib/features/catalog/presentation/widgets/promocion_grupo_field.dart`), un dropdown con presets amigables (Sin promoción / 2x1 / 3x2 / 6x5 / Segundo producto con % dto. / Personalizada) que resuelve a esos mismos 2 campos — elegir "Personalizada" expone los campos crudos directamente. Se usa en `CatalogFormScreen` (alta) y `EditarVarianteDialog` (edición, con `_inferirTipo` reconstruyendo el preset correcto a partir de los valores guardados). Es **mutuamente excluyente** con el descuento por volumen en la misma Variante (mismo criterio que el backend): configurar ambos a la vez muestra un error y no envía el formulario. `PromocionGrupo.etiqueta()` (`lib/features/catalog/domain/models/promocion_grupo.dart`) centraliza el texto de la etiqueta ("2x1", "2do al 40% dto.", etc.) para no duplicar esa lógica entre la tarjeta de producto y la línea del carrito. En el POS, `ProductoResultadoTile` muestra la etiqueta en la tarjeta cuando corresponde (mutuamente excluyente con el aviso de descuento por volumen, igual que en el dominio) y `LineaCarrito.subtotal` aplica el descuento **automáticamente** apenas se completa un grupo — mismo patrón "estimar localmente antes de Cobrar" que el descuento por volumen — mostrando "2x1 aplicado" (o el preset que corresponda) bajo la cantidad de la línea. Verificado con 2 widget tests nuevos (la etiqueta "2x1" aparece en la tarjeta; el Total no cambia con 1 unidad pero sí al agregar la 2ª, mostrando "aplicado") y `flutter analyze` limpio.
- **Descuento general en el POS con autorización de un Supervisor** (a pedido explícito, "descuento general con autorización de un usuario que tenga el perfil de autorizar descuentos" — "podemos dejar ambos tipos de aprobación, si el supervisor puede estar en piso o desde la oficina"): el botón "+ Descuento" del POS (antes deshabilitado, "Próximamente") ahora abre `SolicitarDescuentoDialog` (% o monto, mutuamente excluyentes). A diferencia de los descuentos por línea, acá el carrito **deja de ser 100% local** en cuanto se pide un descuento: `PosCartController.solicitarDescuento` crea la Venta y agrega las líneas en ese momento (no recién al Cobrar) para tener un `VentaId` real contra el cual pedir la autorización — desde ahí el carrito queda bloqueado para edición (`carritoBloqueado`, deshabilita +/-, quitar y Vaciar en `CarritoLineaTile`/el panel) porque las líneas ya viven en el servidor. Mientras el descuento está Pendiente, un banner en el panel del carrito lo indica y el botón "Cobrar" queda deshabilitado — no se puede cobrar un precio que nadie autorizó. El POS **consulta el estado cada 3 segundos** (`Timer.periodic` en `PosScreen`, `PosCartController.verificarEstadoDescuento`) mientras espera: mismo mecanismo sirve tanto si el Supervisor resuelve al toque parado al lado del Cajero como si tarda porque está en la oficina, no hay dos flujos distintos. Nueva pantalla `DescuentosPendientesScreen` (ruta `/descuentos-pendientes`) para quien autoriza: lista las solicitudes con Autorizar/Rechazar (rechazar pide motivo en `RechazarDescuentoDialog`, obligatorio). **Primera vez que el cliente necesita saber los permisos del Usuario logueado**: no existía ningún mecanismo — se decodifica el claim `permiso` del propio Access Token JWT (`lib/core/auth/jwt_permisos.dart`, sin librería externa, solo para mostrar/ocultar UI — la autorización real la sigue haciendo el backend en cada endpoint) y se guarda en `SesionUsuario.permisos`; el ítem "Descuentos pendientes" en Home solo aparece si `sesion.tienePermiso('sales.descuentos.autorizar')`. Verificado con 13 tests nuevos (`PosCartController`: solicitar crea la Venta y reutiliza el mismo `VentaId` en pedidos siguientes, `cobrar` no hace nada mientras Pendiente, `verificarEstadoDescuento` transiciona el estado; `PosScreen`: el diálogo solicita y bloquea Cobrar; `DescuentosPendientesScreen`: lista, autoriza, rechaza con motivo, estado vacío) y `flutter analyze` limpio.
- **Búsqueda en el listado admin de Catálogo** (a pedido explícito, "en el catálogo de productos no está mostrando todos los productos" — investigado y resuelto como un fix de seguridad en el backend, ver su README: faltaba Row-Level Security en `Productos`/`VariantesProducto` y el catálogo mezclaba Productos de otras Empresas; ya corregido, la Empresa ve exactamente los suyos). Con eso resuelto, un catálogo real de 41 Productos sin buscador seguía siendo difícil de recorrer, así que `ProductosAdminScreen` pasó de `ConsumerWidget` a `ConsumerStatefulWidget` y ganó un `TextField` que **filtra en memoria** (no llama al backend de nuevo) por nombre de Producto, SKU o código de barras de cualquiera de sus Variantes — a diferencia de la búsqueda del POS (que sí pagina contra el servidor, ver `BuscarProductosVendiblesQuery`), acá ya se cargó la lista completa de una sola vez (`ProductosAdminController.cargar()`, sin límite ni paginación, "el catálogo de una PyME es chico"), así que filtrar en memoria es instantáneo y no necesita debounce ni tocar el backend. Botón para limpiar la búsqueda, mensaje "Sin resultados" distinto del de "sin Productos creados todavía". Verificado con 4 widget tests nuevos (filtra por nombre, filtra por SKU, sin resultados muestra el mensaje, limpiar vuelve a mostrar todo — y confirma que no se vuelve a llamar `listarProductos()` al filtrar) y `flutter analyze` limpio.
- **Fix: se podían agregar más Productos a una Venta con un descuento ya solicitado/autorizado** (reportado por el usuario probando la feature de arriba): el bloqueo del carrito (`carritoBloqueado`) se implementó en `CarritoLineaTile` (+/-, quitar, editar cantidad) pero se olvidó el punto de entrada real — tocar una tarjeta nueva en la grilla del POS seguía llamando `PosCartController.agregarProducto` sin ningún chequeo, agregando la línea solo en el estado local sin avisarle al backend: el Total en pantalla subía, pero `Confirmar()` iba a cobrar el subtotal viejo (el que el Supervisor autorizó), ignorando el producto agregado de más. Corregido moviendo el chequeo al controller en vez de a cada botón de la UI por separado — `agregarProducto`, `cambiarCantidad` y `quitarLinea` ahora verifican `carritoBloqueado` ellos mismos (no solo los widgets que los llaman), cerrando esta clase de bug de raíz en vez de parchar el síntoma puntual; `agregarProducto` además dejo un mensaje explicando por qué, en vez de fallar en silencio. De paso, a pedido explícito ("avisar que el descuento será borrado"), el botón "Vaciar" dejó de deshabilitarse cuando hay un descuento en curso — ahora sigue clickeable, pero si `estadoDescuento` no es SinSolicitar pide confirmación primero ("Ya se solicitó un descuento para esta venta... tendrás que pedirlo de nuevo") antes de vaciar. `PosCartController.vaciarCarrito()` pasó de solo limpiar `lineas` a reiniciar todo el estado (`ventaId`, `estadoDescuento`, etc.) — antes dejaba el carrito local vacío pero la Venta ya creada en el servidor seguía "enganchada" (`carritoBloqueado` en true para siempre), lo que habría bloqueado agregar productos nuevos sin ninguna forma de salir de ese estado. Verificado con 4 tests nuevos en `PosCartController` (agregar/cambiar/quitar bloqueados, `vaciarCarrito` reinicia todo) y 3 widget tests nuevos en `PosScreen` (tocar un producto nuevo no lo agrega y muestra el aviso; Vaciar con descuento pide confirmación y respeta Cancelar/Confirmar; Vaciar sin descuento no pide nada) — 86 tests en total, `flutter analyze` limpio.
- **"Ver más" en la cola de autorización de descuentos** (a pedido explícito, "identificar los productos que desea comprar el cliente y que cliente"): cada tarjeta de `DescuentosPendientesScreen` gana un botón "Ver más — Cliente y Productos" que expande/colapsa (`_TarjetaDescuentoPendiente` pasó de `StatelessWidget` a `StatefulWidget` con un simple `_expandido`, sin acoplarlo al estado de la lista completa). El detalle **se pide recién al expandir**, no de entrada para toda la cola — `detalleDescuentoPendienteProvider`, un `FutureProvider.autoDispose.family<DetalleDescuentoPendiente, String>` parametrizado por `ventaId`, contra el nuevo `GET /ventas/descuentos-pendientes/{ventaId}` del backend (ver su README) — así que expandir una fila no dispara ni recarga las demás. Mientras carga muestra un spinner chico, y si falla un texto de error inline (la tarjeta y sus botones Autorizar/Rechazar siguen operables, el detalle es solo informativo). El contenido expandido muestra el Cliente (nombre y RUT si tiene) y cada línea como "`{cantidad} × {nombreProducto}`" con su subtotal, usando el mismo `MonedaFormatter` que el resto del carrito. Verificado con 1 widget test nuevo (expandir pide el detalle y lo muestra, incluye Cliente/RUT/línea con cantidad y nombre; volver a tocar el botón colapsa y muestra "Ver más" de nuevo) — 87 tests en total, `flutter analyze` limpio.
- **Filtro por Departamento en el catálogo admin** (a pedido explícito, "una búsqueda similar a punto de venta por departamento"): `ProductosAdminScreen` gana una fila de chips (`_TabsDepartamentos`/`ChoiceChip`) debajo del buscador de texto, mismo patrón visual que `_TabsCategorias` del POS (chip "Todos" + uno por Departamento) pero con los colores por defecto de Material en vez del tema navy del POS, porque esta pantalla no comparte esa identidad visual. Los Departamentos se listan con un provider nuevo y local a Catalog, `departamentosAdminProvider` (en vez de reusar el `departamentosProvider` de Sales, para no hacer depender a Catalog — el feature "más de base" — de un provider de Sales). El filtro por Departamento se combina con el de texto ya existente y sigue siendo **puramente en memoria** — `ProductoAdmin` ahora trae `departamentoId`/`departamentoNombre` resueltos por el backend (ver su README), así que elegir una tab no dispara ningún llamado nuevo, solo reduce la lista ya cargada. Verificado con 4 widget tests nuevos (elegir un Departamento filtra; volver a "Todos" lo quita; se combina con la búsqueda por texto; sin Departamentos no se muestra la fila de tabs) — 91 tests en total, `flutter analyze` limpio.
- **Generación e impresión de código de barras en el catálogo admin** (a pedido explícito, "código de barra" — clarificado como generar/imprimir, no el escaneo que ya existía en el POS): en `EditarVarianteDialog`, una Variante sin código de barras muestra "Generar código de barras" (llama al nuevo endpoint del backend, ver su README, y actualiza el campo con el EAN-13 devuelto); en cuanto hay un código (generado o cargado a mano) aparece una previsualización visual (`VistaPreviaEtiqueta`, con `barcode_widget`) y "Imprimir etiqueta". El botón de imprimir también vive directo en `_VarianteTile` del listado (`productos_admin_screen.dart`) para reimprimir sin abrir el diálogo de edición. La etiqueta (8×5cm — tamaño típico de etiqueta autoadhesiva de góndola, con Nombre/Sku/código/Precio) se arma con `pdf` + `printing` — `pw.BarcodeWidget` dibuja las barras directo en el PDF (no vía SVG, cuyo parser en `pdf` solo soporta un subconjunto limitado — la primera versión pasaba por ahí y encajaba silenciosamente) — y `Printing.layoutPdf` entrega el PDF al diálogo de impresión/vista previa nativo. La simbología se elige validando el dígito verificador EAN-13 real (mismo algoritmo que `GeneradorCodigoBarras` del backend), no solo la longitud — un código de 13 dígitos con checksum inválido cae a Code128 en vez de reventar al intentar codificarlo como EAN-13. **Bug real encontrado y corregido durante la verificación manual** (el usuario reportó "veo el botón pero no hace nada"): en este entorno, el plugin web de `printing` no queda registrado por el mecanismo automático de Flutter (`PrintingPlatform.instance` se quedaba en el `MethodChannelPrinting` por defecto, que depende de canales de plataforma nativos inexistentes en un navegador y fallaba con `Unsupported operation: Platform._operatingSystem` **antes** de siquiera llamar al callback que arma el PDF — confirmado instrumentando el código temporalmente, sin log ni excepción visible en consola hasta ese punto). Corregido registrando el plugin a mano en `main()`: `lib/core/printing/registrar_impresion_web.dart` (con variantes `_stub`/`_web` vía `export ... if (dart.library.html)`, mismo patrón condicional que el resto del código multiplataforma) asigna `PrintingPlatform.instance = PrintingPlugin()` explícitamente en vez de confiar en el auto-registro, sin efecto en Android/Windows (ahí el registro automático sí funciona). Verificado manualmente end-to-end en el navegador: género un código real, la vista previa lo mostró, y el diálogo de impresión abrió con la etiqueta completa (Nombre, Sku, barras, Precio). Verificado también con 5 tests nuevos (botón de generar visible solo sin código; botón de imprimir y previsualización visibles solo con código; generar pide al repositorio y actualiza el campo; error al generar deja el botón disponible; el ícono de imprimir en el listado aparece solo si la Variante tiene código) — 96 tests en total, `flutter analyze` limpio.
- **Imprimir la etiqueta con o sin Precio** (a pedido explícito, "para algunas empresas no imprimen el precio, dejar dos impresiones con y sin precio"): tanto `imprimirEtiquetaCodigoBarras` como `VistaPreviaEtiqueta` ganan un parámetro `mostrarPrecio` (`true` por defecto) — cuando es `false`, el `pw.Text` del Precio simplemente no se agrega a la columna del PDF, en vez de imprimirlo vacío o tachado. En `EditarVarianteDialog` esto se expone como dos botones separados, "Imprimir con precio" / "Imprimir sin precio" (no un checkbox + un solo botón, para que la acción sea inequívoca al tocar); en `_VarianteTile` del listado, el mismo ícono de imprimir ahora abre un `PopupMenuButton` con las dos opciones en vez de imprimir directo. Verificado manualmente en el navegador (ambas opciones abren el diálogo de impresión con el contenido correcto) y con 1 widget test nuevo (el menú del ícono de imprimir ofrece ambas opciones) más 2 tests existentes actualizados para las nuevas keys — 97 tests en total, `flutter analyze` limpio.
- **Burbuja con la cantidad de descuentos pendientes en el Home** (a pedido explícito, para que un Supervisor no tenga que entrar a la cola para saber si hay algo esperando): la tarjeta "Descuentos pendientes" del menú (ya gateada por `sesion.tienePermiso('sales.descuentos.autorizar')`) muestra un `Badge` con la cantidad sobre la flecha cuando `descuentosPendientesProvider.pendientes` no está vacío — nada de badge en cero, mismo criterio "sin novedades, sin ruido visual" que el resto de la app. `HomeScreen` pasó de `ConsumerWidget` a `ConsumerStatefulWidget` para sostener un `Timer.periodic` de 20s que llama `cargar()` mientras el Home está en pantalla (más espaciado que el poll de 3s del POS, porque ahí el Cajero está esperando activamente una respuesta y acá es solo un indicador pasivo) — el timer solo dispara la recarga si la sesión sigue teniendo el permiso, aunque `cargar()` tampoco haría daño sin él. Se reutiliza el mismo `descuentosPendientesProvider` que ya usa `DescuentosPendientesScreen` (no uno nuevo solo para el conteo) — como ambas pantallas comparten la misma instancia mientras el Home sigue montado debajo de la ruta empujada, autorizar/rechazar desde la cola actualiza la burbuja del Home sin lógica adicional. Verificado con 3 widget tests nuevos (sin el permiso no se ve ni la tarjeta ni la burbuja; con el permiso pero sin pendientes se ve la tarjeta sin burbuja; con pendientes se ve la burbuja con la cantidad correcta) — 100 tests en total, `flutter analyze` limpio. Sin cambios en el backend — reutiliza el endpoint de la cola ya existente.
- **Fix: el monto del descuento general no se veía en ningún lado del carrito** (reportado por el usuario: "cuando esté autorizado el descuento se modifica el subtotal, pero nunca se muestra en el detalle antes del subtotal el descuento $"): el descuento autorizado ya afectaba el Total (y por lo tanto el Subtotal/IVA, que se derivan de él vía `ResumenVenta.calcular`), pero esa diferencia era invisible — no había ningún $ que explicara por qué el Subtotal bajó. Nuevo `_FilaDescuentoAplicado` en `pos_screen.dart`, entre las líneas del carrito y el Subtotal, con el mismo formato "Descuento (X%)" o "Descuento" (monto fijo) que ya usaba el banner de estado, mostrando `-$monto` en el color de acento — visible solo cuando `carrito.montoDescuentoAplicado > 0` (es decir, solo con el descuento ya Autorizado; `PosCartState.montoDescuentoAplicado` ya existía, calculado y probado desde la feature original). Verificado con 1 widget test nuevo (sin descuento Autorizado la fila no aparece; tras autorizar un 10% sobre una línea de $1.500, aparece "Descuento (10%)" con "-$150") — 101 tests en total, `flutter analyze` limpio.
- **Cotizaciones en el POS** (a pedido explícito, "venta sin confirmar" — el botón "Cotización" del rediseño visual quedaba deshabilitado con tooltip "Próximamente", ver bullet del rediseño más arriba): el `OutlinedButton` deshabilitado se reemplazó por un `PopupMenuButton` con dos acciones, "Guardar cotización" y "Rescatar cotización" (mismo patrón que el menú con/sin precio de la etiqueta de código de barras). **Guardar**: `PosCartController.guardarCotizacion` crea la Venta y agrega las líneas si aún no existían (mismo patrón lazy que `cobrar()`/`solicitarDescuento()`), llama al nuevo `POST /ventas/{id}/cotizacion` del backend (ver su README) y vacía el carrito — `PosScreen._guardarCotizacion` arma el ticket para imprimir con los datos que ya estaban en pantalla (no hace falta volver a consultar al servidor) y abre el diálogo de impresión nativo vía el nuevo `imprimirTicketCotizacion` (mismo mecanismo `pdf`+`printing` que la etiqueta de código de barras, formato ticket de 8cm con Cliente/líneas/Subtotal/IVA/Total y el aviso "sin valor tributario"). **Rescatar**: `RescatarCotizacionDialog` lista las Cotizaciones vigentes de la Sucursal (`GET /ventas/cotizaciones?sucursalId=...`); al elegir una, si el carrito actual ya tiene productos se pide confirmación antes de reemplazarlo (mismo criterio que "Vaciar" con un descuento en curso), y `PosCartController.rescatarCotizacion` trae el detalle completo (`GET /ventas/cotizaciones/{id}`) y reconstruye el carrito con el carrito ya bloqueado (`ventaId` asignado, igual que cualquier Venta ya persistida) — el precio de cada línea reconstruida es el subtotal ya cotizado dividido por la cantidad (no el precio de lista actual, que puede haber cambiado desde que se guardó la Cotización), para que el Total coincida exactamente con lo impreso al guardar. **Actualización posterior**: el descuento por volumen/promoción de cada línea sí viaja ahora en el detalle y se refleja al rescatar — ver bullet "Número de Cotización y descuentos preservados al rescatar" más abajo. Verificado con 6 tests nuevos en `PosCartController` (guardar con carrito vacío no hace nada; crea/marca/vacía; reusa la Venta si ya existía por un descuento previo; informa el error sin vaciar el carrito; rescatar trae el detalle y bloquea el carrito; rescatar informa el error) y 3 widget tests nuevos en `PosScreen` (el menú deshabilita "Guardar" con el carrito vacío; rescatar reemplaza el carrito con la Cotización elegida; rescatar con el carrito no vacío pide confirmar) — 137 tests en total, `flutter analyze` limpio. **Verificación visual en el navegador no se pudo completar en esta sesión** por la misma limitación de compositing de capturas ya documentada más abajo (el flujo de "Guardar"/"Rescatar" involucra el diálogo de impresión nativo del sistema operativo, que no se puede ejercitar con un widget test) — quedó pendiente de una prueba manual del usuario contra la app real corriendo en `http://localhost:8090`.
- **Número de Cotización y descuentos preservados al rescatar** (a pedido explícito, "la Cotización debería tener una numeración para rescatarla, además cuando la rescata debería reflejarse si está aplicada una promoción por las unidades compradas, por volumen u otras"): `CotizacionResumen`/`CotizacionDetalle` ganan `numeroCotizacion` (`String?`, ej. `COT-20260806-003`, ver README del backend para el correlativo por día en hora de Chile) — se muestra como "`{número} · {Cliente}`" en el título de cada fila de `RescatarCotizacionDialog` (antes solo el Cliente) y como línea centrada bajo "COTIZACIÓN" en el ticket impreso (`imprimirTicketCotizacion`, que también usa el número como nombre del PDF en vez de los primeros 8 caracteres del `ventaId`). **El ticket ahora se imprime con el detalle real del servidor, no con lo que ya estaba en pantalla**: `PosScreen._guardarCotizacion` antes armaba el `CotizacionDetalle` a imprimir reconstruyéndolo localmente desde el carrito — no podía incluir el número (recién se asigna en el servidor al guardar) — ahora hace un `obtenerCotizacion(ventaId)` justo después de guardar y usa ese detalle real para el ticket, simplificando de paso el código (ya no duplica la lógica de qué descuento aplicó cada línea, que ahora vive una sola vez en `LineaCarrito`). **Descuento por línea reflejado al rescatar** (la parte del pedido que era un bug, no solo la numeración): `LineaCarrito` gana `porcentajeDescuentoVolumenHistorico`/`montoDescuentoPromocionHistorico` (`double?`) — hechos ya ocurridos que vienen del backend (`LineaVenta.PorcentajeDescuentoAplicado`/`MontoDescuentoPromocion`, ver su README), distintos de `aplicaDescuentoVolumen`/`aplicaPromocionGrupo`, que siguen siendo el cálculo en vivo contra las reglas *actuales* del catálogo (`producto.cantidadMinimaDescuentoVolumen`, etc.) para una línea recién agregada al carrito. Antes de este fix, `PosCartController.cargarCotizacion` armaba un `ProductoVendible` sintético sin esas reglas de catálogo, así que una línea rescatada con descuento nunca mostraba la etiqueta ("5% dto. por volumen aplicado", "2x1 aplicado") aunque el `Subtotal` ya viniera correctamente rebajado — el monto siempre estuvo bien, solo faltaba el indicador visual. `CarritoLineaTile` ahora chequea primero los campos históricos (si vienen, gana ese texto) antes de caer al cálculo en vivo — para una promoción por grupo no se puede reconstruir el preset exacto ("2x1" vs "segundo al 40%") solo a partir del monto descontado, así que el texto histórico para ese caso es genérico ("Promoción aplicada (-$monto)") en vez de intentar adivinar el preset original. Verificado con 4 tests nuevos en `PosCartController`/`PosScreen` (rescatar preserva el descuento general Autorizado; rescatar preserva `porcentajeDescuentoVolumenHistorico` de una línea; el carrito muestra la etiqueta "5% dto. por volumen aplicado" tras rescatar una Cotización con ese descuento) — 137 tests en total, `flutter analyze` limpio. **El ticket impreso ahora también muestra el descuento** (el usuario probó la feature de arriba y notó que el ticket "no reflejaba la venta como se grabó" — el Total ya incluía el descuento general Autorizado correctamente, pero no había ninguna línea que lo mostrara, así que un 1% de descuento quedaba invisible): `imprimirTicketCotizacion` calcula el monto real (`subtotalLineas - total`, siempre exacto, sin tener que rederivar porcentaje/monto fijo) y agrega una fila "Descuento (X%)"/"Descuento" en verde entre las líneas y el Subtotal — mismo criterio y ubicación que `_FilaDescuentoAplicado` en el carrito — solo si hay descuento aplicado. También agrega, bajo cada línea con promoción, el mismo texto histórico que ya se mostraba en el carrito ("X% dto. por volumen aplicado" / "Promoción aplicada (-$monto)"). **Nota sobre esta verificación**: el bug real que el usuario reportó (número/descuentos ausentes en el ticket y el diálogo de rescatar, fecha mostrada sin corresponder a la hora actual) resultó ser, para el número y el descuento, una build vieja del frontend en el navegador del usuario — verificado contra la API real (`curl`) que el backend ya devolvía `numeroCotizacion`, `estadoDescuentoGeneral` y `montoDescuentoPromocion` correctos para exactamente la misma Cotización que aparecía sin esos datos en pantalla. La fecha sí era un bug real, ver "Fix de raíz" en el README del backend — corregido ahí, no acá, porque el problema era la serialización del lado del servidor, no el parseo en Dart.
- **Fix real: precio unitario incorrecto al rescatar una línea con promoción** (encontrado por el usuario comparando el ticket impreso, ya validado como correcto, contra el mismo carrito recién rescatado: mostraba "x $34.493" en vez de "x $45.990" para un Neumático con "4x3" aplicado): `PosCartController.cargarCotizacion` armaba el `ProductoVendible` sintético con `precioVenta: linea.subtotal / linea.cantidad` — el precio *promedio ya rebajado*, no el precio real que `LineaCotizacionDetalle.precioUnitario` ya traía desde el backend (`LineaVenta.PrecioUnitario`, disponible desde que existe la Cotización, nunca se estaba usando). El Total daba bien porque `LineaCarrito.subtotal` multiplicaba ese precio promedio por la cantidad, así que el promedio "compensaba" al recalcular — pero mostraba un precio unitario que el Cliente nunca pagó. Corregido usando `linea.precioUnitario` directo (el precio real) y ajustando `LineaCarrito.subtotal` para que, cuando vienen los campos históricos (`porcentajeDescuentoVolumenHistorico`/`montoDescuentoPromocionHistorico`), aplique el descuento ya ocurrido sobre ese precio real en vez de recién multiplicar — el cálculo en vivo contra las reglas del catálogo (para una línea recién agregada) queda intacto, sin tocar. Verificado con 1 test nuevo en `PosScreen` (rescatar una Cotización con "4x3" muestra "x $45.990", no el promedio, y el Total sigue coincidiendo exactamente) — 138 tests en total, `flutter analyze` limpio.
- **Fix real: la Unidad de Medida no viajaba al rescatar una Cotización** (encontrado por el usuario mirando el carrito rescatado — "para el precio del pan no se identifica 1.6 son kilos"): `PosCartController.cargarCotizacion` armaba el `ProductoVendible` sintético con `unidadMedida: 0` fijo (Unidad), sin importar la Unidad real de la Variante — un Producto por Kilogramo (ej. Pan Hallulla) rescatado mostraba la cantidad pelada ("1.6") en el carrito, en vez de con su unidad ("1.6 kg", como se ve al agregarlo en vivo). El monto nunca estuvo mal (la cantidad real, 1.6, seguía siendo correcta para el cálculo), solo faltaba el sufijo de unidad en la UI. `LineaCotizacionDetalle` (frontend) gana `unidadMedida` (parseado con el mismo enum `UnidadMedida` ya usado en Catálogo), poblado desde el nuevo campo que expone `ObtenerCotizacionQuery` en el backend (ver su README) — `cargarCotizacion` ahora usa `linea.unidadMedida.valor` en vez del `0` fijo, y `CarritoLineaTile` renderiza la unidad correcta sin cambios, porque ya leía `producto.unidad` de forma genérica. Verificado con 1 widget test nuevo en `PosScreen` (rescatar una Cotización de un Producto por Kilogramo muestra "1.6 kg" en el carrito) — `flutter analyze` limpio, suite completa verde.
- **Fix: la etiqueta de promoción por grupo no se veía igual al rescatar una Cotización** (el usuario probó el fix de Unidad de Medida de arriba y pidió que se viera "igual que antes de grabar la cotización" — el carrito rescatado mostraba "Promoción aplicada (-$45.990)" en vez de "4x3 aplicado" como en una línea recién agregada): `LineaCotizacionDetalle` gana `cantidadPorGrupoPromocion`/`porcentajeDescuentoUnidadPromocion` (poblados desde la Variante por el backend, ver su README — no vienen de un hecho histórico, sino del preset vigente del catálogo). `PosCartController.cargarCotizacion` los pasa al `ProductoVendible` sintético; `CarritoLineaTile` (nuevo helper `_etiquetaPromocionHistorica`) prioriza `PromocionGrupo.etiqueta(...)` cuando el preset viene disponible, y solo cae al texto genérico si la Variante fue desactivada o cambió de promoción desde que se guardó la Cotización — mismo criterio aplicado en `imprimirTicketCotizacion` (nuevo helper `_etiquetaPromocion`) para que el ticket impreso muestre la misma etiqueta. Verificado con 1 widget test nuevo en `PosScreen` (rescatar una Cotización con el preset disponible muestra "4x3 aplicado", no el texto genérico) — `flutter analyze` limpio, suite completa verde (140 tests).
- **Fix: rescatar una Cotización con descuento ya Autorizado mostraba el aviso de "recién autorizado"** (el usuario probó los dos fixes de arriba y notó "cuando rescatas una cotización que tiene un descuento la muestra como que en ese momento fue aprobada"): el listener de `PosScreen` mostraba el SnackBar verde "Descuento autorizado — ya puedes cobrar." cada vez que `estadoDescuento` cambiaba a `Autorizado`, sin distinguir *por qué* cambió — una resolución en vivo (`verificarEstadoDescuento`, un Supervisor recién autorizó uno Pendiente) y `cargarCotizacion` reemplazando todo el `PosCartState` con el de una Cotización que ya traía el descuento Autorizado de antes disparaban el mismo aviso, dando la falsa impresión de que la aprobación acababa de ocurrir. Corregido acotando la condición a la transición real `Pendiente → Autorizado/Rechazado` (`previo?.estadoDescuento == EstadoDescuentoGeneral.pendiente`), que es el único caso donde `verificarEstadoDescuento` cambia el estado — rescatar una Cotización ya resuelta solo actualiza el *banner* persistente (`_BannerDescuento`, texto neutro "Descuento de X% autorizado.", sin problema porque no implica un evento reciente), sin el SnackBar transitorio. Verificado con 1 widget test nuevo en `PosScreen` (rescatar una Cotización con descuento ya Autorizado no muestra el SnackBar) — `flutter analyze` limpio, suite completa verde (141 tests).
- **Buscar por número en "Rescatar Cotización", acotado a las últimas 10** (a pedido explícito, "agrega el buscar por numero en la parte superior", y después "que solo puedas mostrar las ultimas 10 cotizaciones en la lista antes de realizar la busqueda, y despues de realizar la busqueda, no superar los 10 registros"): `RescatarCotizacionDialog` gana un `TextField` sobre el listado que filtra localmente por `NumeroCotizacion` — la lista ya viene completa de `GET /ventas/cotizaciones?sucursalId=...` (solo las vigentes de la Sucursal, un volumen acotado por diseño), así que no hace falta un endpoint de búsqueda aparte, a diferencia de Clientes/Proveedores/Productos donde la lista completa sí sería demasiado grande. Una Cotización guardada antes de que existiera el número (`numeroCotizacion == null`) deja de aparecer mientras haya texto de búsqueda, ya que no hay con qué compararla — mismo caso ya cubierto en el título de la fila. El resultado, con o sin filtro, se acota a 10 (`.take(10)`) — la lista ya viene ordenada por fecha descendente desde el backend (`VentaRepository.ListarCotizacionesAsync`), así que el tope siempre son las 10 más recientes que coincidan, sin necesidad de reordenar nada en el frontend. Verificado con 2 widget tests nuevos en `PosScreen` (dos Cotizaciones, filtrar por un fragmento del número deja solo la que coincide; un texto sin coincidencias muestra "Ninguna cotización coincide con la búsqueda."; con 15 Cotizaciones, la 11 en adelante no aparece ni sin filtro ni filtrando) — `flutter analyze` limpio, suite completa verde (143 tests).
- **Mantención de Clientes** (a pedido explícito, "mantención de Clientes que permita modificar y crear"): nuevo `ClientesAdminScreen` (ruta `/clientes`, tarjeta en Home gateada por `customers.clientes.gestionar`) — listado con buscador (`GET /clientes/buscar?texto=`, mismo debounce que el selector del POS, reutilizando `customerRepositoryProvider` ya existente en `sales/presentation/providers/pos_providers.dart` en vez de duplicarlo) y `ClienteFormDialog` para alta y edición (Rut/Nombre/Email/Teléfono). El `Rut` se deshabilita al editar (igual que el `Sku` en Catálogo — es el identificador, no se reasigna) y se habilita al crear — a diferencia del dominio, donde es opcional (solo para el Cliente Genérico de mostrador, sembrado automáticamente, nunca creado desde este formulario), acá **es obligatorio**: quien registra explícitamente un Cliente nuevo debe identificarlo con su Rut real (a pedido explícito, "si registras a un cliente el rut no puede ser opcional"). **`CupoCredito`/`PlazoPagoDias` deliberadamente fuera de este formulario** (a pedido explícito, tras una primera versión que sí los exponía como campos editables más): otorgar crédito es una decisión de riesgo que no debe quedar mezclada con mantención de datos de contacto — necesita su propia evaluación y autorización aislada (mismo criterio que el descuento general del POS, `sales.descuentos.autorizar`) — diseño pendiente, no implementado todavía. `_guardar()` reenvía el `CupoCredito`/`PlazoPagoDias` existentes del Cliente sin cambios en cada edición (nunca los deriva de un campo de formulario) para no resetearlos por accidente mientras ese flujo no exista. **Completar el Rut de un Cliente que quedó sin uno** (a pedido explícito, tras ver en la app un Cliente real creado antes de que el alta exigiera el Rut): editar un Cliente cuyo `rut` es nulo o vacío habilita el campo (label "RUT (completar)") en vez de dejarlo siempre deshabilitado — al guardar, si se completó, llama al nuevo `PUT /clientes/{id}/rut` (`CustomerRepository.asignarRutCliente`, ver README del backend) además de `actualizarCliente`, validando el mismo formato que en el alta; un Cliente que ya tiene Rut lo sigue mostrando deshabilitado, sin esta vía de escape. Verificado con 8 widget tests nuevos (listado muestra los Clientes; tocar uno precarga el formulario con el Rut deshabilitado y sin campos de crédito; guardar la edición llama a `actualizarCliente`; el FAB abre el alta con el Rut habilitado; alta sin Rut muestra el error y no llama al repositorio; alta con Rut válido lo normaliza y crea el Cliente; editar un Cliente sin Rut habilita el campo vacío para completarlo; completar el Rut llama a `asignarRutCliente` con el Rut normalizado) y contra LocalDB real: el Cliente de prueba `Cliente E2E Actualizado` (creado sin Rut en una verificación anterior del backend) quedó con `76.543.210-3` tras usar el nuevo endpoint.
- **`ClienteFormDialog`: sección "Datos para Factura"** (a pedido explícito, "Hay que cubrir todo lo necesario para luego emitir una factura al cliente que lo necesite" — ver README del backend para el fix completo, que incluyó descubrir que Cuentas por Cobrar ya existía de una sesión anterior y que lo único que realmente faltaba era este dato): nueva sección al final del formulario con Giro/Dirección/Comuna/Ciudad — los 4 campos que el SII exige del receptor de una Factura (una Boleta no los necesita), por eso van marcados "(opcional)" con una nota aclarando cuándo hacen falta, a diferencia del Rut/Nombre que siguen siendo obligatorios al registrar. `ClienteResumen`/`CustomerApi`/`CustomerRepository`/`ClientesAdminController` extendidos con los 4 campos nuevos (mismo patrón que Email/Teléfono, todos nullable). Si el Cajero intenta cobrar una Venta a crédito o con Rut propio sin estos datos completos, el backend responde 400 con un mensaje claro (ver `VentaConfirmadaDteHandler` en el README del backend) — este formulario es donde se completan antes de que eso pase. Verificado con 3 widget tests nuevos en `ClientesAdminScreen` (alta envía los 4 campos al repositorio; alta sin completarlos los envía como `null`, confirmando que son opcionales; editar un Cliente que ya los tenía los precarga en el formulario) — `flutter analyze` limpio, suite completa verde (149 tests).
- **Flujo completo de compra** (a pedido explícito, "generar un flujo completo de compra con todos sus pasos"): nuevo feature `purchasing/` completo, siguiendo la misma Clean Architecture que el resto de la app (modelos/repositorio en `domain/`, API client + implementación en `data/`, controllers/pantallas en `presentation/`) y reutilizando `tenancyRepositoryProvider`/`catalogRepositoryProvider` de `sales/presentation/providers/pos_providers.dart` (mismo criterio de reuso de providers entre features que ya usa Clientes) para resolver Bodegas y Productos al armar una Orden. **Proveedores** (`ProveedoresScreen`, ruta `/compras/proveedores`): listado con buscador y alta/edición, mismo patrón que Clientes (`ProveedorFormDialog`, Rut deshabilitado al editar). **Órdenes de Compra** (`OrdenesCompraScreen`/`OrdenCompraDetalleScreen`, rutas `/compras/ordenes` y `/compras/ordenes/:id`): listado con chips de filtro por Estado (Borrador/Enviada/Recibida, contra el nuevo `GET /ordenes-compra` del backend — ver su README) y un detalle que expone las 3 acciones del ciclo de vida según el Estado actual, nunca las 3 a la vez: "Agregar línea" y "Enviar" solo en Borrador, "Recibir" solo en Enviada, ningún botón de acción en Recibida. `RecibirOrdenCompraDialog` lista cada línea con su cantidad pendiente y permite recibir parcial o totalmente (reutiliza el mismo endpoint de recepción parcial ya construido en sesiones previas del backend). Las líneas se muestran con el Nombre de Producto y Sku ya resueltos por el backend, no solo el Id de la Variante (ver extensión de `ObtenerOrdenCompraQuery` en el README del backend). **Documentos Recibidos y Discrepancias** (`DocumentosRecibidosScreen`/`DiscrepanciasScreen`, esta última accesible también desde `compras_hub_screen.dart` y desde el listado de Proveedores vía `state.extra`): `RegistrarDocumentoRecibidoDialog` registra el documento tributario real (Boleta/Factura) contra una Orden ya Recibida o de forma directa (compra sin Orden formal, ej. útiles de oficina — mismo criterio que el backend); `DiscrepanciasScreen` lista las diferencias detectadas automáticamente (monto del documento vs. negociado) con `ResolverDiscrepanciaDialog` exigiendo un motivo para cerrarlas, igual que el backend. `compras_hub_screen.dart` es la pantalla de entrada del feature (ruta `/compras`, tarjeta en Home gateada por `purchasing.proveedores.gestionar` **o** `purchasing.ordenescompra.gestionar` — cualquiera de los dos permisos, no ambos, porque un Usuario que solo gestiona Proveedores igual necesita ver el menú de Compras). Verificado con 17 widget tests nuevos (Proveedores: listado, búsqueda, alta, edición; Órdenes: listado, filtro por Estado, navegación al detalle; Detalle de Orden: botones correctos por Estado, enviar, recibir con cantidad pendiente precargada; Home: gating de las tarjetas nuevas por permiso) contra un `FakePurchasingRepository`, y contra el backend real vía `curl` el ciclo completo end-to-end: Proveedor creado y editado, Orden creada → línea agregada → Enviada → filtrada correctamente por Estado → Recibida por completo → Documento Recibido registrado por un monto distinto al negociado → Discrepancia detectada automáticamente con la diferencia exacta ($4.000 sobre $1.000 de umbral). **Verificación visual en el navegador no se pudo completar en esta sesión**, misma limitación de compositing ya documentada más abajo — pendiente de una prueba manual del usuario contra la app real en `http://localhost:8090`.
- **Precio de oferta por rango de fecha, en Catálogo y el POS** (a pedido explícito, "tenemos como dejar un precio oferta en un rango fecha?" — ver README del backend para la decisión de hacerlo mutuamente excluyente con volumen/grupo): nuevo widget reutilizable `OfertaField` (`lib/features/catalog/presentation/widgets/oferta_field.dart`, mismo patrón que `PromocionGrupoField`) con un campo de precio y dos `ListTile` que abren `showDatePicker` para "Vigente desde"/"Vigente hasta" — usado en `CatalogFormScreen` (alta) y `EditarVarianteDialog` (edición). **Exclusión mutua del lado del cliente** (mismo criterio ya usado entre volumen y grupo): configurar Oferta junto con volumen o grupo muestra un error y no envía el formulario, para no dejar que el usuario llegue al 400 del backend sin explicación. `ProductoVendible`/`VarianteAdmin` ganan `precioOferta`/`ofertaDesde`/`ofertaHasta` (`DateTime?`, parseados de la fecha `yyyy-MM-dd` que devuelve el backend) y `ProductoVendible` gana los getters `ofertaVigente` (compara la fecha del dispositivo, solo para mostrar — la resolución real siempre es del servidor, ver `VarianteProducto.PrecioVigente` en el backend) y `precioEfectivo` (oferta si está vigente, precio normal si no). **El carrito nunca necesitó lógica de exclusión propia**: `LineaCarrito.subtotalSinDescuento` pasó de `producto.precioVenta` a `producto.precioEfectivo * cantidad` — como Oferta es mutuamente excluyente con volumen/grupo en el backend, cuando hay oferta esos otros campos siempre vienen `null`, así que el descuento por volumen/promoción de grupo simplemente no encuentra nada que aplicar, sin ningún `if` adicional. En el POS, `ProductoResultadoTile` muestra el precio normal tachado sobre el precio de oferta en acento (`Column`, no `Row` — un `Row` desbordaba la tarjeta angosta de 148px, encontrado y corregido por el propio widget test) con el badge "Oferta" (mismo lugar que los badges de volumen/grupo, mutuamente excluyente con ellos igual que en el dominio) y `CarritoLineaTile` usa `precioEfectivo` en el precio unitario mostrado bajo la cantidad. **Verificado en vivo por el usuario contra la app real** (`http://localhost:8090`) — funcionaba, pero señaló que la línea del carrito no dejaba claro que el precio venía de una oferta ("no se nota que lo compraste en oferta"): se agregó la etiqueta "Oferta aplicada" bajo la cantidad de la línea (mismo lugar y estilo que "X% dto. por volumen aplicado"/"2x1 aplicado", condicionada a `producto.ofertaVigente`, en acento naranja para diferenciarla visualmente de esas dos que usan verde). **El usuario probó el ajuste y pidió, además de la etiqueta, que el precio normal tachado también se viera en la línea del carrito** (no solo en la tarjeta de búsqueda) — `CarritoLineaTile` ahora muestra `linea.producto.precioVenta` tachado justo antes del precio unitario ("x $precioEfectivo"), en ambas variantes de línea (por Unidad y por Kilogramo/Litro), con el precio efectivo resaltado en acento y negrita cuando hay oferta vigente, mismo criterio visual que `ProductoResultadoTile`. Verificado con 3 widget tests nuevos (la tarjeta muestra el badge "Oferta" y el precio rebajado; agregar el producto cobra el precio de oferta, muestra "Oferta aplicada" y el precio normal tachado en el carrito) y `flutter analyze` limpio, suite completa verde (149 tests). **El usuario probó también "Rescatar cotización" e imprimir el ticket, y notó que la Oferta no quedaba reflejada en ninguno de los dos** (la Cotización guardada mostraba el precio ya rebajado correctamente, pero sin ningún indicador — el mismo hueco que ya se había resuelto para volumen/grupo en sesiones anteriores). `LineaCotizacionDetalle` (modelo y backend, ver su README) gana `precioOferta`/`ofertaDesde`/`ofertaHasta`, resueltos de la Variante vigente. `LineaCarrito` gana `ofertaAplicadaHistorico` (bool, no un valor monetario — a diferencia de los campos históricos de volumen/grupo, acá no hay fórmula que recalcular, solo confirmar que el precio ya cobrado coincide con la Oferta configurada: `linea.precioOferta == linea.precioUnitario`), poblado en `PosCartController.cargarCotizacion` y renderizado en `CarritoLineaTile` con la misma etiqueta "Oferta aplicada" que la línea en vivo — sin precio tachado en este caso, porque `LineaCotizacionDetalle` no trae el precio normal de la Variante, solo el ya cobrado (mismo criterio que el resto de los indicadores históricos, que tampoco reconstruyen una comparación visual completa). `imprimirTicketCotizacion` recibe el mismo tratamiento, agregando "Oferta aplicada" en verde bajo la línea correspondiente. Verificado con 1 widget test nuevo (rescatar una Cotización con Oferta ya aplicada muestra "Oferta aplicada" en el carrito) y `flutter analyze` limpio, suite completa verde (150 tests). **El usuario probó el ticket impreso y confirmó "Oferta aplicada", pero notó que la línea rescatada en el carrito ("así estaba y cuando la recuperé") no mostraba el precio normal tachado, a diferencia de la línea agregada en vivo**: `LineaCotizacionDetalle` (modelo, ver README del backend) gana `precioVenta` — el precio normal de la Variante, junto a `precioOferta` ya expuesto, para poder mostrar el "antes" tachado también al rescatar/imprimir, no solo el precio ya cobrado. `LineaCarrito` gana `precioNormalHistorico` (double?, solo para mostrar — nunca se usa en el cálculo del Subtotal, que sigue basado en el precio ya cobrado) y `CarritoLineaTile` unifica el criterio de "vigente en vivo" (`producto.ofertaVigente`) y "aplicada históricamente" (`ofertaAplicadaHistorico`) en una sola variable `muestraOferta`, así que el mismo bloque de UI (precio tachado + precio efectivo en acento) sirve para ambos casos, sin duplicar la lógica de renderizado. `imprimirTicketCotizacion` recibe el mismo tratamiento — el precio normal tachado ahora aparece antes de "cantidad x precio" en la línea del ticket cuando corresponde. Verificado con 1 widget test extendido (rescatar una Cotización con Oferta ya aplicada muestra también el precio normal `$500.000`) y `flutter analyze` limpio, suite completa verde (151 tests). **Verificación visual en el navegador**: no se pudo completar por automatización en esta sesión (misma limitación de compositing ya documentada más abajo), pero el usuario probó manualmente contra la app real, incluyendo la impresión, y confirmó los ajustes en cada ronda.
- **Fix visual: el precio tachado no debía cambiar el color del número, solo de la línea** (el usuario probó el amarillo pedido y notó "quedo malo la linea era de color amarillo pero la cifra debiera de haberse mantenido igual"): `color`/`decoration`/`decorationColor` son propiedades independientes de `TextStyle` — la primera pasada dejó `color: PosColors.precioTachado` en el mismo `TextStyle` que `decoration: TextDecoration.lineThrough`, así que el amarillo pintaba tanto la línea como los dígitos, perdiendo el contraste sutil que tenía el gris muted original. Corregido separando ambas propiedades: `color: PosColors.textMuted` (como antes) + `decorationColor: PosColors.precioTachado` (`Color(0xFFD9B31C)`, nuevo en `PosColors`) — el número se ve igual que siempre, solo la línea que lo tacha es amarilla. Aplicado en las 3 ubicaciones (`ProductoResultadoTile` y las 2 variantes de línea en `CarritoLineaTile`). `flutter analyze` limpio, suite completa verde (149 tests).
- **Aviso de nueva versión disponible** (a pedido explícito, tras la pregunta "si desde el navegador instalo el aplicativo, no se actualiza automático cuando esté una nueva versión?"): esta app **no registra Service Worker** (ver el comentario ya existente en `web/index.html` — un SW se colgaba y dejaba `flutter_secure_storage` roto), así que a diferencia de una PWA típica, una instancia ya abierta en el navegador (instalada o no) nunca detecta sola que hay un build nuevo publicado — el usuario tendría que adivinar que debe recargar. Nuevo mecanismo, solo-web (mismo patrón condicional `dart.library.html` que ya usa `registrar_impresion_web.dart` — no-op en Android/Windows): `ActualizacionController` (`lib/core/update/actualizacion_providers.dart`) guarda al abrir la app el contenido de `flutter_service_worker.js` (Flutter lo regenera en cada `flutter build web` con los hashes reales del build — no se usa `version.json`, que solo cambia si se bumpea el pubspec a mano) y lo vuelve a pedir cada 10 minutos (`Timer.periodic`, con `Cache-Control: no-cache` y un query param de cache-busting); si el contenido cambió, `BannerActualizacion` (envuelve toda la app vía `MaterialApp.router.builder`, así que aparece sobre cualquier pantalla) muestra una franja fija con un botón "Actualizar" que recarga la página — sin botón para cerrarla sin recargar, y sin forzar el reload solo (podría cortar una venta a medio hacer). Verificado con 2 widget tests nuevos (sin actualización no se ve la franja; con actualización se ve y el botón dispara el reload) y `flutter analyze` limpio, suite completa verde (151 tests).
- **Home**: menú con acceso a Punto de Venta, Catálogo, Clientes y Compras — el resto de las pantallas (Inventario, etc.) se agregan como nuevos features siguiendo el mismo patrón que `auth/`, `sales/`, `catalog/`, `customers/` y `purchasing/`.

Verificado con la app real corriendo contra `NovaPOS.Api` real (LocalDB) sirviendo en Web (`flutter run -d web-server`, CORS habilitado): la pantalla de Login renderiza, la navegación a Registro de Empresa funciona, y la escritura en los campos de texto se confirmó real (no simulada) inspeccionando el estado real de los widgets. La interacción de tap final contra el botón de envío no se pudo verificar en vivo en el navegador por una limitación de la herramienta de automatización de esta sesión (el compositing de capturas de pantalla no funcionaba) — el flujo completo (llenar formulario → validar → llamar al repositorio con los datos exactos → mostrar el diálogo de éxito → volver a Login) quedó verificado en cambio con **widget tests reales** (`flutter test`) que ejercitan el árbol de widgets de producción completo (hit-testing real sobre el botón, `Form.validate()` real, `AuthController`/`RegistroEmpresaController` reales), con un `AuthRepository` fake reemplazando solo la capa de red — mismo criterio que los `*RepositorioFalso` del backend.

## Tests

```bash
flutter test
```

Tests unitarios de validación de RUT y del controller de clasificación en cascada de Catálogo (`ProviderContainer`, sin pump de widgets), más widget tests de Login, Registro de Empresa, Punto de Venta y administración de Catálogo que cubren el camino feliz, validación de formulario, manejo de errores del backend, idempotencia de sesión, búsqueda con debounce, filtro por categoría, badge de stock, selector de Cliente, armado del carrito, la secuencia completa de cobro, y listado/activar/desactivar/navegación de Catálogo — todos usando repositorios fake, sin red real ni `flutter_secure_storage` real (el canal de plataforma no existe bajo `flutter test`; ver `SecureStorage`/`InMemorySecureStorage`). **No cubierto por widget tests** (documentado como limitación, no como pendiente silencioso): la interacción real con los `DropdownButtonFormField` de `CatalogFormScreen`/`ClasificacionCascade` ni los diálogos `EditarProductoDialog`/`EditarVarianteDialog` — la lógica que importa (`CatalogFormController`) sí está cubierta directamente.

## Qué falta

- El resto de las pantallas (Inventario, etc.) — Login, Registro de Empresa, Punto de Venta, Catálogo, Clientes y Compras son la base sobre la que se construye el resto.
- Crear un Cliente nuevo desde el propio selector del POS (el backend ya soporta `POST /clientes`) — hoy solo se puede elegir entre Clientes ya existentes, o crearlo aparte desde `ClientesAdminScreen`.
- **Cupo de crédito del Cliente**: `CupoCredito`/`PlazoPagoDias` se sacaron a propósito de `ClienteFormDialog` — otorgar crédito necesita su propio flujo aislado de evaluación y autorización (mismo criterio que el descuento general del POS), no un campo libre en la mantención general. Diseño pendiente, ver "Mantención de Clientes" más arriba.
- Agregar una Variante adicional a un Producto ya existente desde la app (el backend ya soporta `POST /catalogo/variantes`) — hoy solo se crea la primera Variante junto con el Producto.
- Editar/quitar una línea de una Venta ya agregada en el backend — hoy el carrito del POS es puramente local por esta razón (ver arriba).
- Offline-first real: SQLite local + sincronización contra el Sync Engine del backend (`POST /sync/lotes`, ya construido del lado del servidor) — hoy la app requiere conexión.
- Publicación real en Android (Play Store) y empaquetado del instalable de Windows (MSIX) — por ahora solo se corre en modo desarrollo.
